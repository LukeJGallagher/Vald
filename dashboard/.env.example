# VALD API Configuration Example
# Copy this file to '../vald_api_pulls-main/forcedecks/.env' and fill in your actual values
# NEVER commit the actual .env file to version control

# VALD API OAuth Credentials
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
TOKEN_URL=https://security.valdperformance.com/connect/token

# Organization Details
TENANT_ID=your_tenant_id_here

# Manual Token (from VALD Hub)
# This token expires periodically and needs to be refreshed
MANUAL_TOKEN=your_jwt_token_here

# API Region
# Options: euw (Europe West), use (US East), aue (Australia East)
VALD_REGION=euw

# API URLs (usually don't need to change these)
PROFILES_API_URL=https://prd-euw-api-externalprofile.valdperformance.com/api/v1/profiles/
GROUPNAMES_API_URL=https://prd-euw-api-athletes.valdperformance.com/v2020q3/team/your_tenant
