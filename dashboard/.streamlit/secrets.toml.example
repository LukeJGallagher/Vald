# Streamlit Secrets Configuration
# Copy this file to secrets.toml and fill in your credentials
# NEVER commit secrets.toml to version control!

# For Streamlit Cloud deployment:
# 1. Go to your app settings in Streamlit Cloud
# 2. Click "Secrets" in the left sidebar
# 3. Paste the contents below with your actual credentials

# =============================================================================
# OPTION 1: Private GitHub Repository (Recommended for historical data)
# =============================================================================
# Store data in a private repo, access from public dashboard
[github]
GITHUB_TOKEN = "ghp_your_personal_access_token"
DATA_REPO = "your-username/vald-data"  # Your private data repository

# =============================================================================
# OPTION 2: Direct VALD API (Live data, last 90 days only)
# =============================================================================
[vald]
# VALD API Credentials - Get these from your VALD Hub account
CLIENT_ID = "your_client_id_here"
CLIENT_SECRET = "your_client_secret_here"
TENANT_ID = "your_tenant_id_here"

# Region: "euw" (Europe), "use" (US East), "aue" (Australia)
VALD_REGION = "euw"

# Optional: Manual token (if using token-based auth instead of OAuth)
# MANUAL_TOKEN = "your_bearer_token_here"

# =============================================================================
# DATA LOADING PRIORITY:
# 1. Local CSV files (for local development)
# 2. Private GitHub repo (if [github] configured) - has all historical data
# 3. VALD API direct (if [vald] configured) - last 90 days only
# =============================================================================
